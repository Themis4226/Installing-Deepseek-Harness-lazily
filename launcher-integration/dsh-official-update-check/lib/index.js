/**
 * Host half of the maintainer-only official update checker.
 *
 * The package contributes no Node-side capability. DSH serves the browser
 * half declared in package.json after this bundle is installed in the local
 * maintainer profile.
 */
export function apply() {}
